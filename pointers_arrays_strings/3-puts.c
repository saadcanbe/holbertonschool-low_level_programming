#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include "main.h"
/**
 * _puts:print a string
 * @str:pointer to the string
 * Return:0 success
 */
void _puts(char *str)
{
_puts(str);
return (0);
}
